spring:
  application:
    name: security-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8070/config
      username: configuser
      password: mys3cr3tpassw0rd
      failFast: true